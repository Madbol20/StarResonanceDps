<UserControl
    x:Class="StarResonanceDpsAnalysis.WPF.Controls.SkillBreakdown.SkillStatsSummaryPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:StarResonanceDpsAnalysis.WPF.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:SkillStatsViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="StatCardBorder" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Margin" Value="8,0" />
            <Setter Property="Padding" Value="0,20" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style x:Key="StatValueText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#1F1F1F" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>

        <Style x:Key="StatLabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#8C8C8C" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>

        <Style x:Key="StatSubText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#BFBFBF" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>
    </UserControl.Resources>

    <UniformGrid Rows="1" Columns="5">
        <!--  Total  -->
        <Border Style="{StaticResource StatCardBorder}">
            <StackPanel>
                <TextBlock Style="{StaticResource StatValueText}" Text="{Binding Total, StringFormat={}{0:N0}}" />
                <TextBlock Style="{StaticResource StatLabelText}" Text="{Binding TotalLabel}" />
                <TextBlock Style="{StaticResource StatSubText}">
                    <Run Text="0.0% " />
                    <Run Text="{lex:Loc Key=SkillBreakdown_Suffix_Progress}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!--  Average  -->
        <Border Style="{StaticResource StatCardBorder}">
            <StackPanel>
                <TextBlock Style="{StaticResource StatValueText}" Foreground="#2563EB" Text="{Binding Average, StringFormat={}{0:N0}}" />
                <TextBlock Style="{StaticResource StatLabelText}" Text="{Binding AverageLabel}" />
                <TextBlock Style="{StaticResource StatSubText}">
                    <Run Text="0 " />
                    <Run Text="{lex:Loc Key=SkillBreakdown_Suffix_Casts}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!--  Hits  -->
        <Border Style="{StaticResource StatCardBorder}">
            <StackPanel>
                <TextBlock Style="{StaticResource StatValueText}" Text="{Binding Hits, StringFormat={}{0:N0}}" />
                <TextBlock Style="{StaticResource StatLabelText}" Text="{Binding HitsLabel}" />
                <TextBlock Style="{StaticResource StatSubText}">
                    <Run Text="0 " />
                    <Run Text="{lex:Loc Key=SkillBreakdown_Suffix_Progress}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!--  Crit Rate  -->
        <Border Style="{StaticResource StatCardBorder}">
            <StackPanel>
                <TextBlock Style="{StaticResource StatValueText}" Foreground="#10B981" Text="{Binding CritRate, StringFormat={}{0:P1}}" />
                <TextBlock Style="{StaticResource StatLabelText}" Text="{Binding CritRateLabel}" />
                <TextBlock Style="{StaticResource StatSubText}" Foreground="#10B981">
                    <Run Text="{Binding CritCount, Mode=OneWay}" />
                    <Run Text=" " />
                    <Run Text="{lex:Loc Key=SkillBreakdown_Suffix_Crits}" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!--  Lucky Count  -->
        <Border Style="{StaticResource StatCardBorder}">
            <StackPanel>
                <TextBlock Style="{StaticResource StatValueText}" Text="{Binding LuckyCount, StringFormat={}{0:N0}}" />
                <TextBlock Style="{StaticResource StatLabelText}" Text="{Binding LuckyCountLabel}" />
                <TextBlock Style="{StaticResource StatSubText}">
                    <Run Text="{Binding LuckyRate, StringFormat={}{0:P1}, Mode=OneWay}" />
                    <Run Text=" " />
                    <Run Text="{lex:Loc Key=SkillBreakdown_Suffix_Lucky}" />
                </TextBlock>
            </StackPanel>
        </Border>
    </UniformGrid>
</UserControl>
